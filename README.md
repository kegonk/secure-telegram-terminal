# 🤖 Telegram Terminal Bot (Local Version)

Безопасный Telegram-бот для локального управления терминалом через tmux сессии с полным мониторингом и системой безопасности.

## ✨ Основные возможности

- 🔐 **Безопасное управление терминалом** через Telegram
- 🛡️ **Многоуровневая система безопасности** с валидацией команд
- ⌨️ **Удобная постоянная клавиатура** с быстрыми командами
- 📊 **Полный мониторинг** и логирование всех действий
- 💻 **Локальная работа** без Docker (прямой доступ к системе)
- 🔄 **Автозапуск** при перезагрузке компьютера
- 🔔 **Уведомления** о статусе бота

## 🚀 Быстрый запуск

### 1. Настройка окружения
```bash
# Создайте .env файл с настройками
BOT_TOKEN=your_telegram_bot_token
ALLOWED_CHAT_ID=your_telegram_chat_id
TMUX_SESSION=claude
LOG_FILE=logs/claude_terminal.log
```

### 2. Установка и запуск
```bash
# Автоматическая установка и запуск
./start_local.sh

# Или для настройки автозапуска:
./setup_autostart.sh      # SystemD (рекомендуется)
./setup_cron_autostart.sh # Crontab
```

### 3. Использование
Отправьте `/start` в Telegram для получения кнопок управления

## 📁 Структура проекта

### Основные файлы
```
├── bot.py                    # Основной код бота (адаптирован для локальной работы)
├── config.py                 # Конфигурация и настройки
├── security.py               # Система безопасности
├── logger.py                 # Система логирования
├── metrics.py                # Сбор метрик и статистики
├── history.py                # История команд
├── notifications.py          # Система уведомлений
├── text_utils.py             # Обработка терминального вывода
└── requirements.txt          # Python зависимости
```

### Скрипты запуска и автозапуска
```
├── start_local.sh            # Локальный запуск бота
├── setup_autostart.sh       # Настройка SystemD автозапуска
├── setup_cron_autostart.sh  # Настройка Crontab автозапуска
├── start_bot_cron.sh        # Скрипт для cron
├── stop_bot.sh              # Остановка бота
└── telegram-terminal-bot.service # SystemD сервис
```

### Документация
```
├── LOCAL_SETUP.md           # Подробная инструкция по установке
├── FAQ.md                   # Часто задаваемые вопросы
└── README.md                # Этот файл
```

### Данные и логи
```
└── logs/                    # Директория логов
    ├── bot.log             # Логи бота
    ├── commands.log        # История команд
    ├── claude_terminal.log # Логи терминала
    ├── cron_bot.log       # Логи cron запуска
    ├── bot_error.log      # Ошибки systemd
    ├── metrics.json       # Метрики в JSON
    └── command_history.json # История с метаданными
```

## ⌨️ Команды и кнопки

### Постоянная клавиатура
```
[📄 /tail] [🔍 /screenshot] [📊 /stats]
[↩️ Enter] [⬇️ Down] [⬅️ Left] [➡️ Right]  
[🔄 /status] [📜 /history] [⚡ /buttons]
```

### Описание команд
- **📄 /tail** - Последние строки из логов терминала
- **🔍 /screenshot** - Текущий вид tmux панели
- **📊 /stats** - Статистика работы системы
- **🔄 /status** - Статус бота и tmux сессии
- **📜 /history** - История команд
- **⚡ /buttons** - Показать кнопки управления

### Навигационные клавиши
- **↩️ Enter** - Отправить Enter в терминал
- **⬇️ Down** - Стрелка вниз
- **⬅️ Left** - Стрелка влево  
- **➡️ Right** - Стрелка вправо

## 🔄 Автозапуск

### SystemD (рекомендуется для Linux)
```bash
# Автоматическая настройка
./setup_autostart.sh

# Управление
sudo systemctl start telegram-terminal-bot
sudo systemctl stop telegram-terminal-bot
sudo systemctl status telegram-terminal-bot
```

### Crontab (альтернатива)
```bash
# Автоматическая настройка
./setup_cron_autostart.sh

# Управление
./start_bot_cron.sh  # Запуск
./stop_bot.sh        # Остановка
```

## 🛡️ Система безопасности

### Многоуровневая защита
1. **Access Control** - только разрешенный chat_id
2. **Command Validation** - блокировка опасных команд
3. **Pattern Detection** - regex анализ подозрительных паттернов
4. **Confirmation System** - подтверждение системных команд
5. **Timeout Protection** - лимиты времени выполнения
6. **Spam Protection** - антиспам защита

### Заблокированные команды
```python
# Деструктивные операции
rm -rf, sudo rm, chmod 777, mkfs, dd if=

# Fork bombs
:(){ :|:& };:

# Системные команды
sudo chmod, sudo chown, format, fdisk, wipefs
```

## 📊 Мониторинг

### Логи
- **bot.log** - События бота
- **commands.log** - Все выполненные команды
- **claude_terminal.log** - Полный вывод терминала
- **cron_bot.log** - Логи автозапуска через cron
- **Security events** - Блокированные команды

### Метрики
- Количество выполненных команд
- Статистика ошибок
- Время работы системы
- Активность пользователей
- Популярные команды

## 🔧 Управление

### Запуск и остановка
```bash
# Ручной запуск
./start_local.sh

# Остановка
pkill -f "python.*bot.py"
./stop_bot.sh  # Если используете cron

# SystemD управление
sudo systemctl start telegram-terminal-bot
sudo systemctl stop telegram-terminal-bot
```

### Проверка статуса
```bash
# Проверка процесса
pgrep -f "python.*bot.py"

# Проверка tmux сессии
tmux list-sessions | grep claude

# SystemD статус
sudo systemctl status telegram-terminal-bot
```

## ⚙️ Настройка

### Требования
- **Python 3.7+**
- **tmux** - для создания сессий терминала
- **curl** - для отправки уведомлений

### Установка зависимостей

**Ubuntu/Debian:**
```bash
sudo apt install tmux curl python3 python3-pip
```

**CentOS/RHEL:**
```bash
sudo yum install tmux curl python3 python3-pip
```

**macOS:**
```bash
brew install tmux curl python3
```

### Переменные окружения (.env)
```bash
BOT_TOKEN=your_telegram_bot_token
ALLOWED_CHAT_ID=your_telegram_chat_id
TMUX_SESSION=claude
LOG_FILE=logs/claude_terminal.log
```

### Получение chat_id
1. Отправьте любое сообщение боту
2. Откройте: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. Найдите `"id"` в разделе `"chat"`

## 🔔 Уведомления

Бот автоматически отправляет уведомления о:
- ✅ Запуске системы
- ❌ Остановке или сбоях
- 🚨 Критических ошибках
- 📊 Статусе системы

## 🚨 Устранение неполадок

### Проблемы с tmux
```bash
# Проверить сессии
tmux list-sessions

# Создать сессию заново
tmux new-session -d -s claude

# Подключиться к сессии
tmux attach -t claude
```

### Проблемы с запуском
```bash
# Проверить логи
tail -f logs/bot.log
tail -f logs/cron_bot_error.log

# Проверить виртуальное окружение
source venv/bin/activate
python bot.py
```

### Переустановка окружения
```bash
rm -rf venv
./start_local.sh
```

## 📈 Отличия от Docker версии

| Параметр | Docker | Локально |
|----------|--------|----------|
| **Работа** | В контейнере | Прямо на системе |
| **Установка** | `docker-compose up` | `./start_local.sh` |
| **Автозапуск** | Встроенный | Требует настройки |
| **Зависимости** | В образе | Venv локально |
| **Доступ к системе** | Ограниченный | Полный |
| **Обновление** | Пересборка | Обновление кода |
| **Производительность** | Контейнер | Нативная |

## 🤝 Поддержка

Если у вас возникли проблемы:
1. Проверьте файл `.env` с настройками
2. Убедитесь что tmux установлен: `tmux -V`
3. Проверьте логи: `tail -f logs/bot.log`
4. Пересоздайте venv: `rm -rf venv && ./start_local.sh`
5. Прочитайте FAQ.md и LOCAL_SETUP.md

## 📋 FAQ

**Q: Бот работает локально или через Docker?**
A: Теперь бот работает **ЛОКАЛЬНО** на вашем компьютере, без Docker.

**Q: Запустится ли бот после перезагрузки?**
A: По умолчанию НЕТ. Используйте `./setup_autostart.sh` или `./setup_cron_autostart.sh`

**Q: Как проверить что бот работает?**
A: `pgrep -f "python.*bot.py"` или `sudo systemctl status telegram-terminal-bot`

---

**⚡ Готов к работе!** Отправьте `/start` в Telegram для начала управления локальным терминалом.