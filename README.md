# 🤖 Telegram Terminal Bot

Безопасное управление терминалом через Telegram с мониторингом и системой безопасности.

## 🚀 Быстрый запуск

1. **Настройка** - создайте `.env`:
```bash
BOT_TOKEN=your_telegram_bot_token
ALLOWED_CHAT_ID=your_telegram_chat_id
```

2. **Запуск**:
```bash
./start.sh
```

3. **Использование** - отправьте `/start` в Telegram

## 📁 Структура

```
├── bot.py              # Основной код
├── config.py           # Конфигурация
├── security.py         # Система безопасности
├── logger.py           # Логирование
├── metrics.py          # Метрики
├── history.py          # История команд
├── notifications.py    # Уведомления
├── text_utils.py       # Обработка текста
├── start.sh            # Запуск
├── setup_autostart.sh  # Автозапуск
└── stop_bot.sh         # Остановка
```

## ⌨️ Команды

- **📄 /tail** - последние строки терминала
- **🔍 /screenshot** - текущий вид tmux
- **📊 /stats** - статистика
- **🔄 /status** - статус системы
- **📜 /history** - история команд

## 🔧 Автозапуск

```bash
# SystemD (рекомендуется)
./setup_autostart.sh

# Управление
sudo systemctl start/stop/status telegram-terminal-bot
```

## 🛡️ Безопасность

- Контроль доступа по chat_id
- Блокировка опасных команд  
- Подтверждение системных операций
- Защита от спама

## 📊 Логи

- `logs/bot.log` - основные логи
- `logs/claude_terminal.log` - терминал
- `logs/command_history.json` - история
- `logs/metrics.json` - метрики

---
**⚡ Готов к работе!** Отправьте `/start` в Telegram.