# Часто задаваемые вопросы (FAQ)

## ❓ Бот работает локально или через Docker?

**Ответ:** Бот теперь работает **ЛОКАЛЬНО** на вашем компьютере, а не через Docker.

Изменения:
- ✅ Удален Docker-специфичный код 
- ✅ Настроен для работы с локальным tmux
- ✅ Использует виртуальное окружение Python
- ✅ Работает напрямую с терминалом локальной машины

## ❓ Запустится ли бот автоматически после перезагрузки?

**Ответ:** **НЕТ**, по умолчанию бот НЕ запускается автоматически.

### Для автозапуска выберите один из способов:

**🔧 Способ 1 - SystemD (рекомендуется):**
```bash
./setup_autostart.sh
```

**⏰ Способ 2 - Crontab:**
```bash
./setup_cron_autostart.sh
```

**👤 Способ 3 - При входе пользователя:**
Добавьте в `~/.bashrc`:
```bash
cd /путь/к/боту && ./start_local.sh &
```

## ❓ Как проверить, что бот запущен?

```bash
# Проверка процесса
pgrep -f "python.*bot.py"

# Для systemd
sudo systemctl status telegram-terminal-bot

# Проверка tmux сессии
tmux list-sessions | grep claude
```

## ❓ Где хранятся логи?

- `logs/bot.log` - основные логи бота
- `logs/claude_terminal.log` - логи терминала  
- `logs/bot_error.log` - ошибки (при использовании systemd)
- `logs/cron_bot.log` - логи cron (при использовании crontab)

## ❓ Как остановить бота?

**Ручной запуск:** `Ctrl+C` в терминале

**SystemD:** `sudo systemctl stop telegram-terminal-bot`

**Crontab:** `./stop_bot.sh`

**Принудительно:** `pkill -f "python.*bot.py"`

## ❓ Что делать при ошибках?

1. Проверьте файл `.env` с настройками
2. Убедитесь что tmux установлен: `tmux -V`
3. Проверьте логи: `tail -f logs/bot.log`
4. Пересоздайте venv: `rm -rf venv && ./start_local.sh`

## ❓ Отличия от Docker версии?

| Параметр | Docker | Локально |
|----------|--------|----------|
| Установка | `docker-compose up` | `./start_local.sh` |
| Автозапуск | Есть | Требует настройки |
| Зависимости | В контейнере | Venv локально |
| Доступ к системе | Ограниченный | Полный |
| Обновление | Пересборка образа | Обновление кода |